import { Injectable } from "@angular/core";
import { Http } from "@angular/http";
import { Photographer } from "./photographer.model";
import { Observable } from "rxjs";


@Injectable()
export class PhotographerService {
    constructor(private _http: Http) { }

    public add(entity: Photographer) {
        return this._http
            .post(`${this._baseUrl}/api/photographer/add`, entity)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get() {
        return this._http
            .get(`${this._baseUrl}/api/photographer/get`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public getById(options: { id: number }) {
        return this._http
            .get(`${this._baseUrl}/api/photographer/getById?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public remove(options: { id: number }) {
        return this._http
            .delete(`${this._baseUrl}/api/photographer/remove?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get _baseUrl() { return ""; }

}
