import { Injectable } from "@angular/core";
import { Http } from "@angular/http";
import { ContactMessage } from "./contact-message.model";
import { Observable } from "rxjs";


@Injectable()
export class ContactMessageService {
    constructor(private _http: Http) { }

    public add(entity: ContactMessage) {
        return this._http
            .post(`${this._baseUrl}/api/contactmessage/add`, entity)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get() {
        return this._http
            .get(`${this._baseUrl}/api/contactmessage/get`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public getById(options: { id: number }) {
        return this._http
            .get(`${this._baseUrl}/api/contactmessage/getById?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public remove(options: { id: number }) {
        return this._http
            .delete(`${this._baseUrl}/api/contactmessage/remove?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get _baseUrl() { return ""; }

}
